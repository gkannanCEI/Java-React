spring:
  datasource:
    url: jdbc:postgresql://localhost:5435/pos_db
    username: postgres
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true